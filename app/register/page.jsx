'use client'; import Link from 'next/link'; import {useState} from 'react'; import {supabase} from '@/lib/supabase'
export default function Register(){const [name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');setMsg('');const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name,phone}}});if(error)setError(error.message);else setMsg(data.session?'Account created.':'Account created. Please check your email to confirm your account.');setBusy(false)}return <section className="auth"><div className="authCard"><span className="eyebrow">CREATE ACCOUNT</span><h1>Join Taleem Tech</h1><p>Create your account to save purchases in your library.</p><form onSubmit={submit}><label>Full name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Phone<input value={phone} onChange={e=>setPhone(e.target.value)}/></label><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" minLength="6" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}{msg&&<div className="success">{msg}</div>}<button disabled={busy} className="btn primary full">{busy?'Creating…':'Create Account'}</button></form><p className="center">Already registered? <Link href="/login">Sign in</Link></p></div></section>}
