'use client'; import Link from 'next/link'; import {useState} from 'react'; import {supabase} from '@/lib/supabase'
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);else location.href='/library';setBusy(false)}return <section className="auth"><div className="authCard"><span className="eyebrow">WELCOME BACK</span><h1>Sign in</h1><p>Access your purchases and customer library.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button disabled={busy} className="btn primary full">{busy?'Signing in…':'Sign in'}</button></form><p className="center">New here? <Link href="/register">Create an account</Link></p></div></section>}
